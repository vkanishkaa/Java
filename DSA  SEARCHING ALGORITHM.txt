DSA : SEARCHING ALGORITHM 

	It is used to find the location of the element in inside the datastructure (array)
TYPES :
	1.linear search : it checks each element one by one until the given element is found .example : searching student name in attendance list
	

 class LinearSearch{
    public static void main string args[] {
        int arr[]=[10,20,30,40,500];
        int found =30;
        for(int i=0;i<arr.length;i++)
        {
            if(arr[i]==found)
            {
                System.out.println(x:"Element is found");
                return;
            }
        }
        System.out.println(x:"Element is  not found");
    }
}
	2.binary search : it find the element repeatedly by dividing sorted array into half 

	public class BinarySearch {
    public static void main(String args[]) {

        int arr[] = {10, 20, 30, 40, 50};
        int found = 30;

        int low = 0;
        int high = arr.length - 1;

        while (low <= high) {
            int mid = (low + high) / 2;

            if (arr[mid] == found) {
                System.out.println("Element is found");
                return;
            }
            else if (arr[mid] < found) {
                low = mid + 1;
            }
            else {
                high = mid - 1;
            }
        }

        System.out.println("Element is not found");
    }
}

	3.sorting algorithm: arrange elements in ascending or decending order
TYPES:
1.Bubble sort: Repeatedly compares adjacent elements if they are in wrong order
2.Selection sort 
3.Insertion sort
4.Quick sort
